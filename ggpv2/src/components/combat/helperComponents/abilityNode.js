import React from 'react';

export default class AbilityNode extends React.Component {
    render() {
        return (
            <div>
                {this.props.name}
            </div>
        )
    }
}